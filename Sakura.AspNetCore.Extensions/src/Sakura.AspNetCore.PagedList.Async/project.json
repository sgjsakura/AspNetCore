{
	"version": "1.0.0",
	"title": "ASP.NET Core PagedList Async Extension Library",
	"description": "This package provides extension method to create PagedList using EntityFramework async method.",

	"packOptions": {
		"summary": "This package provides extension method to create PagedList using EntityFramework async method.",
		"releaseNotes": "First release",
		"authors": [ "Iris Sakura" ],
		"owners": [ "Iris Sakura" ],
		"tags": [ "ASP.NET", "ASP.NETCore", "Page", "Paging", "Cache", "Caching" ],
		"projectUrl": "https://github.com/sgjsakura/AspNetCore",
		"licenseUrl": "https://github.com/sgjsakura/AspNetCore/blob/master/LICENSE.txt"
	},

	"dependencies": {
		"Sakura.AspNetCore.PagedList": "2.0.0",
		"Microsoft.EntityFrameworkCore": "1.0.0"
	},

	"frameworks": {
		"netstandard1.3": {},
		"net451": {}
	},

	"configurations": {
		"Debug": {
			"buildOptions": {
				"optimize": false,
				"xmlDoc": false
			}
		},
		"Release": {
			"buildOptions": {
				"optimize": true,
				"xmlDoc": true
			}
		}
	},

	"scripts": {
		"postcompile": [ "dotnet pack --no-build -c Release" ]
	}
}
